# Raw data — organization's source extracts, never committed
spend-analysis/data/raw/

# Derived / cached data — regenerable, can be large
spend-analysis/data/processed/
*.parquet

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Local tmp logs
*.log

# Claude Code local settings — may contain API keys, environment overrides
.claude/settings.local.json

# Streamlit local secrets — production password lives in Streamlit Cloud's secrets manager
.streamlit/secrets.toml

# Large derived deliverables — regenerable via classifier_JHK3.py --batch
outputs/NIGP_Mapping_JHK3.csv
outputs/NIGP_Mapping_Review_Queue_JHK3.csv
